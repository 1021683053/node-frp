var version = require('../lib/util/version.js');

describe("version 测试!", function(){
	it('获取当前使用版本和下载数据', function(done){
		// version('v0.9.1')
		// .then(function(res){
		// 	console.log(res);
		// 	done();
		// })
		// .catch(function(err){
		// 	done( new Error(err) );
		// });
		done();
	});
});